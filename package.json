{
  "name": "mongodb-upgrade-toolkit",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "release": "npm publish --access public -w packages/cli -w packages/mcp"
  }
}
